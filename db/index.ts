import TmpGql from './gql'
import TmpQueries from './queries'
import TmpSchema from './schema'

export const gql = TmpGql
export const Queries = TmpQueries
export const Schema = TmpSchema

export default {
  gql,
  Queries,
  Schema
}
